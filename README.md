# BogoSort Analysis

simulates bogoSort and shows that normalized attempts (attempts / n!) follow an exponential distribution.

## usage

```
python main.py
```

Input:

* n → array size (keep small, e.g. ≤ 8)
* t → number of trials

## Requirements

```
pip install matplotlib numpy
```
